package ca.teamdman.sfm.client.terminal;

/** Alpha interpretation for raw terminal raster pixels. */
public enum SFMTerminalRasterAlphaMode {
    STRAIGHT,
    /** Representable for explicit v1 rejection and possible future contracts. */
    PREMULTIPLIED
}
