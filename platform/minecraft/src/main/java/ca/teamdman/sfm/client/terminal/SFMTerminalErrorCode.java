package ca.teamdman.sfm.client.terminal;

/** Stable SFM copy of the structured Vox terminal error discriminator. */
public enum SFMTerminalErrorCode {
    INVALID_REQUEST,
    UNSUPPORTED_CAPABILITY,
    SESSION_NOT_FOUND,
    CAPACITY_EXCEEDED,
    CANCELLED,
    DISCONNECTED,
    INTERNAL
}
