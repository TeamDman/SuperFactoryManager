// Newly inserted line for the explicit migration walkthrough.
// Disposable live-review walkthrough: approve, then change the return value.
class Example {
    int value() {
        return 2;
    }
}
